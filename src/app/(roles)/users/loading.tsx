"use client";
import Loader from "@/components/loader/Loader";
import React from "react";

const Loading = () => {
  return (
    <>
      <Loader loading={true} />
    </>
  );
};

export default Loading;
